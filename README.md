# mad

follow the steps to use the code:
step 1 - the full main dart code is available with the name of the exercise.
step 2 - for yaml file at end of each main drat code, you can see it in comments and copy that and paste in pubspec.yaml file.
step 4 - change the name in the yaml file as your app name and before running the main dart check the intendation(mostly crt for every one exepct mood_tracker app) and press get pub
step 3 - run the main dart to get the desired output of the file.
